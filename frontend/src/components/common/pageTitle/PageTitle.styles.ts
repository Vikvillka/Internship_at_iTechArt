import { SxProps, Theme } from '@mui/material';

export const pageTitleStyles = {
  titleBox: {
    display: 'flex',
    alignItems: 'center',
    height: 40,
    mb: 2,
    justifyContent: 'space-between',
  } as SxProps<Theme>,
};
