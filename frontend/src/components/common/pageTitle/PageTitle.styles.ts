import { SxProps, Theme } from '@mui/material';

export const pageTitleStyles = {
  titleBox: {
    display: 'flex',
    alignItems: 'center',
    mb: 4,
    justifyContent: 'space-between',
  } as SxProps<Theme>,
};
